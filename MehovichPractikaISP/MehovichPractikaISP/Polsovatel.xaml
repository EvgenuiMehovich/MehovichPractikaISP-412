<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MehovichPractikaISP.Polsovatel">
    <ScrollView>

        <StackLayout>
            
            <Frame BackgroundColor="#222222" Padding="24" CornerRadius="0">
                <Label Text="Выбор темы" HorizontalTextAlignment="Center" TextColor="White" FontSize="36"/>
            </Frame>
            <ScrollView>
            <StackLayout>
                    <ListView>
                        <ListView.ItemTemplate >
                            <DataTemplate>

                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <Button Margin="20" CornerRadius="15" BackgroundColor="#222222"
                Text="Вернуться назад" TextColor="White"
                x:Name="Back" Clicked="Back_Clicked"/>
            </StackLayout>
            </ScrollView>
        </StackLayout>
    </ScrollView>
</ContentPage>